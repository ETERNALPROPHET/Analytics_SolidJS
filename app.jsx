import { createSignal, For } from 'https://cdn.skypack.dev/solid-js';import { render } from 'https://cdn.skypack.dev/solid-js/web';async function App(){const res=await fetch('data.json');const d=await res.json();const [v]=createSignal(d.visitors);return <main><For each={v()}>{(n,i)=><span class='kpi'>Day {i()+1}: {n}</span>}</For></main>}render(App,document.getElementById('root'));